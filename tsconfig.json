{
  // The leading / is important to ensure that only the /types folder in the
  // root directory is included, and not any other /types folders e.g. in
  // node_modules
  "include": ["/types/*.d.ts"],
  "references": [
    {
      "path": "./packages/helpers"
    }
  ]
}
