const assert = require("assert");
const curriculumHelpers = require("../index.js");

describe("curriculumHelpers", () => {
  it("has a test", () => {
    assert(curriculumHelpers, "curriculumHelpers should be an object");
  });
});
